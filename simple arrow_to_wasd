import time
import keyboard

key_pair_list = [["w", "up"], ["a", "left"], ["s", "down"], ["d", "right"]]
for key_pair in key_pair_list:
    keyboard.remap_key(key_pair[0], key_pair[1])

while True:
    time.sleep(10)
